/**
 * Page-side extraction function. Serialized and evaluated inside the browser:
 * walks the subtree, prunes default styles, serializes to IR.
 * Must be self-contained (no imports).
 */
export declare const EXTRACT_FN_SOURCE: string;
